# أسرار — لا تُرفع أبدًا
.env
*.key

# مخرجات تشغيل محلية
trading-desk/logs/
**/logs/
__pycache__/
*.pyc

# فك مؤقت لحزمة الوكلاء (الأصل مضغوط في المستودع)
bundle-extracted/

# حالة المهام المحلية (تُنشأ عند التشغيل) — النماذج المرجعية في jobs/ تبقى بدونها
trading-desk/jobs/**/status.json
